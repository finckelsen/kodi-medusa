{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd kodiprint-medusa && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd kodiprint-medusa && npm run start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}